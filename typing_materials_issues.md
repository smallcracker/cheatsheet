# Typing Materials 审计结果

练习页面只展示 `description`，随机抽取条目，并用 `state.typed === state.current.command` 做完整字符串匹配。因此每个条目现在都使用一条可在单行输入框中直接输入的固定命令，描述明确说明参数、变量名、引号和操作符。

下表编号使用修复前 417 条题库的顺序；同一 item 可能同时属于多个问题。

| 问题 | 对应的原 item |
| --- | --- |
| 一个 command 包含多个候选写法 | 107、110、197、211、220、254、296、305、312-314、328、346、351、371、417 |
| PDB 使用语法模板而非具体输入 | 192-198、204-214、219-223、225-227、275-280 |
| PDB 操作语义重复 | 204/276、205/277、206/278 |
| 参数或占位符不确定 | Git 74、81-84、91；Linux 113-117、119-121、126、128、132、144-146、155、169、172-173；PDB 196-198、204-210、213-214、217-223、225-226、236-237、239-240、242、257-261、266-267、275-280；PyTorch 284-285、315-316、319-324、329-332、339、341-342、344-349、352、354、366、380、387、398-399、417 |
| 引号或特殊字符未完整说明 | 34、62、94、122、124-125、130-131、155、169、234-235、271、307、312-313、367、395-396、400-401、411 |
| 管道、重定向及复合操作符未完整说明 | 46、55、129、131、136、187、201-203、225、235-236、241、249、263、280、326-328、335、399 |
| 概念记法被当作可输入命令 | 107-110、298-302、305、342、345-347、381、383、394、414-417 |
| 变量名没有在描述中明确指出 | 170-171、357-360、364-365、398-399、406、410 |
| JSON 与页面数据可能再次不一致 | 不对应单个 item，属于生成流程问题 |

本轮按原题库顺序检查并修复了以下问题：

- 候选写法拆成独立条目：Git 的 `main / master`、PDB 的 `run / restart`、PyTorch 的设备迁移、形状查询、矩阵乘法和激活函数等。
- PDB 语法模板改为具体命令：缩写、方括号、竖线、`...`、`bpnumber`、`expression` 等不再出现在练习命令中；重复的 `clear`、`disable`、`enable` 模板已删除。
- 未确定的参数改为固定示例：提交 `abc1234`、进程号 `12345`、文件名、环境名、包名、模型名、张量变量名和函数参数均在 command 与 description 中对应一致。
- 引号和操作符写入描述：双引号、嵌套引号、单引号、`|`、`>`、`&&`、分号和 `@` 等需要精确匹配的字面量均有说明。
- 概念记法改为可输入代码：类型别名、位布局、模块通配符和索引示意已删除或改为固定示例；不完整的 `class`、`with`、`for` 代码已改成单行形式。
- Docker 续行命令保持为空格连接的单行命令，未保留反斜杠或换行。

`typing_materials.json` 是唯一数据源。运行 `node generate_typing_materials.js` 可生成页面实际加载的 `typing_materials.js`，运行 `node generate_typing_materials.js --check` 可检查两者是否同步，并验证数量、空值、重复项、模板符号、单行限制及特殊字符描述。

当前验证结果：

- JSON 与 JS 均可解析，JavaScript 语法有效。
- 条目数为 421，`count` 与数组长度一致。
- 无空 command/description、换行命令、续行反斜杠、重复 command 或重复 description。
- 无候选写法、PDB 缩写模板、方括号参数模板、尖括号占位符或省略号模板。
- `node generate_typing_materials.js --check` 通过。
