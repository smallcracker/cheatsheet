以下是图片中Linux命令速查表的内容提取：


### **1. File Commands**
#### `ls [options] file` - 列出目录内容
- `-lha`: 显示隐藏文件，显示大小，修改时间等。


#### `tree [options] dir` - 以树形结构显示目录
- `-h`: 显示文件大小
- `-L max`: 限制最大目录深度
- `-a`  显示隐藏文件


#### `cp [options] source dest` - 复制文件或目录
- `-b`: 覆盖前备份
- `-r`: 递归复制（目录）
- `-i`: 覆盖前交互确认


#### `mv [options] source dest` - 移动或重命名文件
- `-b`: 覆盖前备份
- `-f`: 强制覆盖
- `-i`: 覆盖前交互确认


#### `ln [options] file link` - 创建链接
- `-s`: 创建软链接（默认硬链接）
- `-f`: 覆盖已存在的链接
- `-b`: 覆盖前备份

**示例:软链接 vs 硬链接**
```bash
# 创建原文件
echo "Hello" > original.txt

# 创建硬链接(文件实体副本,指向同一inode)
ln original.txt hardlink.txt
# 修改任意一个,另一个也会改变(因为指向同一数据)
rm original.txt  # 删除原文件,硬link仍可用

# 创建软链接(快捷方式,存储原文件路径)
ln -s original.txt softlink.txt
# 删除原文件后,软link会失效(红色闪烁)
# 软link可跨文件系统,硬link不可以
```

**应用场景:**
- 软链接: 管理多版本程序 `ln -s python3.11 python`
- 硬链接: 重要文件备份(不占用额外空间)


  | 特性       | 软链接             | 硬链接              |
  | ---------- | ------------------ | ------------------- |
  | 本质       | 快捷方式(存储路径) | 文件别名(同一inode) |
  | 删除原文件 | 失效❌              | 仍可用✅             |
  | 跨文件系统 | ✅ 可以             | ❌ 不可以            |
  | 链接目录   | ✅ 可以             | ❌ 不可以            |
  | 占用空间   | 很小(几字节)       | 不占空间            |

#### `rm [options] file` - 删除文件或目录
- `-f`: 强制删除
- `-i`: 交互确认
- `-r`: 递归删除（目录）
- `-rf`: 强制递归删除


#### `chmod [options] mode files` - 修改文件权限
- `755`: rwxr-xr-x(常用脚本/程序)
- `644`: rw-r--r--(常用普通文件)
- `+x`: 添加执行权限
- `-R`: 递归修改目录权限

```bash
chmod +x script.sh      # 使脚本可执行
chmod 755 program       # 所有者全权限,其他只读执行
chmod 644 file.txt      # 所有者读写,其他只读
chmod -R 755 dir/       # 递归修改目录权限
```


#### `find [path] [options]` - 查找文件
- `-type f/d`: 仅搜索 文件(f) 或 目录(d)
- `-mtime -7`: 查找最近 7 天内修改过的文件
- `-exec cmd {} \;`: 对匹配文件执行命令（如 `chmod +x`）

**实用示例:**
```bash
find . -name "*.py"             # 找当前目录下所有 python 文件
find . -iname "*log*"           # 找文件名含 log 的文件(不区分大小写)
find . -type f -size +100M      # 找大于 100M 的文件
find . -name "*.tmp" -delete    # 找到并直接删除所有 .tmp 文件
```


#### `cat [file]` - 查看/合并文件
- `-n`: 显示行号
- `>` / `>>`: 用于文件内容覆盖或追加
- **场景**: 快速看小文件内容，或者合并多个文件：`cat a.txt b.txt > all.txt`

#### `tail` / `head` - 查看文件首尾
- `tail -f`: **最常用**，实时追踪文件末尾更新（看日志神器）
- `tail -n 20`: 查看最后 20 行（可简写为 `tail -20`）
- `head -n 10`: 查看前 10 行
- **场景**: `tail -f access.log` 监控网站实时访问情况

#### `cut` - 按列提取
- `-d ':'`: 指定分隔符（默认是 tab）
- `-f 1,3`: 提取第 1 和第 3 列
- **场景**: 提取用户列表 `cut -d: -f1 /etc/passwd`

#### `sort` & `uniq` - 排序与去重
- `sort -n`: 按数字大小排序
- `sort -r`: 逆序排序
- `sort -h`: 按照人类可读数字排序（如 2K, 1G）
- `uniq -c`: 去重并显示每行出现的次数
- **经典组合**: `sort | uniq -c | sort -nr`（排序 -> 统计频率 -> 按频率降序）

#### **综合案例: 文本处理流水线**
```bash
# 实时监控日志并只看包含 "ERROR" 的行
tail -f app.log | grep "ERROR"
```


#### `tar` - 归档与压缩
- **解压**: `tar -zxvf file.tar.gz` (解压到当前目录)
- **压缩**: `tar -zcvf backup.tar.gz dir/` (把文件夹压成 .tar.gz)


#### `du` - 查看磁盘占用
```bash
du -sh .             # 查看当前文件夹总大小
du -sh *             # 查看当前目录下每个文件/文件夹的大小
du -sh * | sort -hr  # 进阶：按大小排序
```

#### `df` - 查看磁盘挂载/剩余
- `df -h`: **最常用**，查看各个磁盘分区的总量、已用和剩余空间
- `df -i`: 查看 inode 使用情况 (如果空间没满但报空间不足，通常是文件太多 inode 耗尽了)
- `df -T`: 显示文件系统类型 (如 ext4, xfs)

**💡 `df` vs `du` 怎么选？**
- **用 `df` 查“整体”**: 确认是哪个硬盘分区满了（宏观）。
- **用 `du` 查“具体”**: 确认是分区里哪个文件夹或文件占用了空间（微观）。
- **经典排查流程**: 先 `df -h` 看哪个分区满 -> `cd` 到该挂载点 -> `du -sh *` 找大目录。

#### `diff` - 比较差异
```bash
diff -u old.py new.py    # 经典的差异对比（Git 风格），可读性最高
diff -y a.txt b.txt      # 左右分栏对比，直观对比修改
diff -qr dir1/ dir2/     # 只看两个目录下哪些文件不同，不看具体内容
```


### **2. Process Commands**


#### `top` - 实时系统监控
`top` 进去后不是死的，可以直接按键操作：
- **P**: 按 **CPU** 使用率排序 (默认)
- **M**: 按 **内存** 使用率排序 (排查内存泄漏神器)
- **N**: 按 PID 排序
- **c**: 显示完整的命令行参数
- **k**: 杀掉进程 (输入 PID 即可)
- **q**: 退出

**实用组合:**
- `top -u root`: 只看 root 用户的进程
- `top -p 1234`: 只盯着 PID 为 1234 的进程看

> **💡 进阶建议**: 如果系统里有 `htop`，直接用 `htop`，界面更友好，支持鼠标点击。


### **3. Network & Remote**
#### `ssh [options] user@host ["cmd1;cmd2"]` - 远程登录
- `-X`: 转发X11窗口（如 `ssh -X user@host`），之后可以显示图形界面程序（如 plt 画的图）
- `-p port`: 指定端口号（默认22）


#### `scp` & `rsync` - 远程传输
- **`scp`**: 简单拷贝单个文件
  - `scp -v file.txt user@host:/path/` (上传，显示详细进度)  
  - `scp -v -P 2222 user@host:/path/file.txt .` (指定端口下载，显示详细进度)
- **`rsync`**: **强烈推荐**，支持断点续传、增量同步
  - `rsync -avzP dir/ user@host:/path/` (上传，同步整个目录)
  - `rsync -avzP user@host:/path/ ./data` (下载，同步整个目录)
  - `rsync -avzP -e 'ssh -p 2222' ./data user@host:/path/` # 如果目标端口不是 22


#### `wget` - 网络下载
- `wget -c URL`: **断点续传**下载文件 (常用)
- `wget -O filename URL`: 下载并重命名

**💡 `scp` vs `wget` 怎么选？**
- **选 `scp` (私有传输)**: 你有远程服务器的账号密码/密钥，想在**自己的两台机器**之间传文件。
- **选 `wget` (公开下载)**: 你有一个 **URL 链接**（比如下载安装包、图片），不需要登录服务器系统，直接从网上拉取。



### **2. Network & Diagnostics** (网络与排查)

#### `ip` - 新一代网络工具
> 💡 别再用 `ifconfig` 和 `netstat` 了，它们已经被淘汰。
- `ip -br a`: 查看 IP 地址 (Addr)
- `ip r`: 查看路由表 (Route)
- `sudo ip route add <目标网段> via <下一跳网关> dev <出接口>`：临时添加路由表
- `sudo ip route del <目标网段> via <下一跳网关> dev <出接口>`：删除路由表

#### `ss` - 查看监听端口
- `ss -tunlp`: 查看所有监听端口及对应的进程，关注local一列。
  - `t`: TCP, `u`: UDP, `n`: 数字显示端口, `l`: 监听中, `p`: 显示进程名

#### `ping` & `mtr` - 连通性测试
- `ping -c 4 google.com`: 发送 4 个包后停止 (Linux 默认会一直 ping)
- `mtr google.com`: **神器**，结合了 ping 和 traceroute，动态显示每一跳的丢包率和延迟

#### `nc` (netcat) - 网络瑞士军刀
- `nc -zv 192.168.1.1 80`: **最常用**，扫描/探测对方端口是否开放
- `nc -l 8080`: 临时监听 8080 端口 (用于测试防火墙或接收数据)

#### `dig` - DNS 查询
- `dig google.com`: 查询域名的详细解析记录
- `dig @8.8.8.8 google.com`: 指定使用 Google 的 DNS 进行解析
- `dig +trace google.com`: 显示完整的递归解析路径（从根到权威服务器）

#### `ufw` - 简单防火墙 (Ubuntu/Debian)
- `ufw status`: 查看防火墙状态
- `sudo ufw default deny incoming`: 拒绝所有入站连接
- `sudo ufw default allow outgoing`: 允许所有出站连接
- `sudo ufw allow ssh` 或 `sudo ufw allow 22`: 允许 SSH 端口
- `sudo ufw allow 80/tcp`: 允许 HTTP
- `sudo ufw allow 443/tcp`: 允许 HTTPS
- `sudo ufw allow from 192.168.1.100 to any port 3306`: 仅允许指定 IP 访问本机 3306 端口
- `sudo ufw enable`: 启用防火墙（先确保已允许 SSH）
- `sudo ufw status numbered`: 查看带编号的规则列表
- `sudo ufw delete 2`: 删除编号为 2 的规则（根据上一步编号）

#### `curl` - 网络请求
- `-v`: 显示详细的请求/响应信息，包括 Header、状态码等。
- `-i`: 显示响应 Header
- `curl -i -H "Host: example.com" http://IP:PORT`：
  - 发送一个 HTTP 请求到 `IP:PORT`，并指定 Host 为 `example.com`。
  - 这在测试 **虚拟主机** 或 **多域名** 网站时非常有用，因为服务器通过 Host Header 来判断要响应哪个站点。
- `curl -v cip.cc`: 查看 IP 信息，包括 ISP、位置等。可以用于检查代理是否生效。

#### **网络配置管理 (NetworkManager)**
- `nmtui`: **最推荐**，图形化界面配置网络 (Wifi/IP)，甚至不需要鼠标
- `nmcli`: 命令行配置
  - `nmcli dev wifi connect "SSID" password "PASS"`: 连接 Wifi
- `netplan apply`: (Ubuntu Server) 应用 YAML 网络配置，Yaml 文件通常保存在/etc/netplan文件夹当中
#### `tcpdump` - 网络抓包
- `sudo tcpdump -i eth0 dst 1.2.3.4 -n -ttt`：
  - 监听 eth0 接口，显示目标 IP 为 1.2.3.4 的所有包。
  - `-n`: 不解析 IP 地址为域名
  - `-ttt`: 显示时间戳 (秒级)
- `sudo tcpdump -i any host 1.2.3.4 -w debug.pcap`：
  - 监听任何接口，将目标 IP 为 1.2.3.4 的包保存到 debug.pcap 文件中。
  - 可以用 Wireshark 等工具打开 debug.pcap 查看详细包内容。

#### 配置代理
### **3. Proxy & Env** (代理与环境)

#### **终端代理 (Temporary)**
- `export http_proxy=http://127.0.0.1:7890`
- `export https_proxy=http://127.0.0.1:7890`
- `export all_proxy=socks5://127.0.0.1:7890`
- **取消代理**: `unset http_proxy https_proxy all_proxy`

#### **Git 代理**
- **设置**:
  ```bash
  git config --global http.proxy http://127.0.0.1:7890
  git config --global https.proxy http://127.0.0.1:7890
  ```
- **取消**:
  ```bash
  git config --global --unset http.proxy
  git config --global --unset https.proxy
  ```

#### **Python工具代理**
- **Pip**: `pip install xxx --proxy http://127.0.0.1:7890`
- **Conda**:
  - `conda config --set proxy_servers.http http://127.0.0.1:7890`
### **4. System & Hardware Info** (系统与硬件)

#### **OS & Kernel** (系统版本)
- `cat /etc/os-release`: **最通用**，查看发行版信息 (Ubuntu/CentOS/Debian 都支持)
- `uname -r`: 查看内核版本 (Kernel)
- `uname -a`: 查看所有系统信息 (内核、架构、主机名)
- `hostnamectl`: 查看主机名及架构信息

#### **CPU & Memory** (处理器与内存)
- `lscpu`: 查看 CPU 架构、核数、主频
- `free -h`: 查看内存使用情况 (Human readable)

#### **GPU & AI** (显卡与 CUDA)
- `nvidia-smi`: **最常用**，查看显卡型号、显存占用、驱动版本
- `nvidia-smi -l 1`: 每秒刷新一次 (类似 top)
- `nvitop`: **强烈推荐**，比 `nvidia-smi` 更强大，支持颜色显示和历史曲线
- `nvcc -V`: 查看 **CUDA Toolkit** 编译器的版本
- `cat /usr/local/cuda/version.txt`: 另一种查看 CUDA 版本的方法

#### **Disk & USB** (硬件挂载)
- `lsblk`: 查看所有块设备 (硬盘、分区) 的树状结构
- `fdisk -l`: 查看硬盘分区表详情
- `lsusb`: 查看 USB 设备列表