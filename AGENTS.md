# 代理使用指南

## 终态导向的代码修改原则

修改代码时，请把代码库视为“当前系统的完整表达”，而不是历史变更的档案。

你的目标不是在现有代码上叠加一个能工作的补丁，而是让修改后的代码看起来像需求从一开始就是这样设计的：结构自然、命名一致、没有历史包袱，也不留下本次修改的考古痕迹。

执行任何新增、修改、重构或删除任务时，遵循以下规则：

1. 以最终设计为目标
   - 直接把现有实现整理成符合新需求的最终形态。
   - 可以重写、合并、移动或删除旧实现，不要只在外围叠加条件分支、包装器、适配器或特殊情况。
   - 不要机械追求最小 diff；应追求满足需求前提下最简单、最一致的最终代码。
   - 同时避免与任务无关的大范围重写。

2. 不在代码中记录变更历史
   - 不添加“这里曾经……”“为了兼容旧实现……”“该功能已删除/修改”等注释。
   - 不保留 old/new、legacy、deprecated、v2、temporary 等过渡性命名，除非这些概念确实属于当前业务模型。
   - 修改原因和历史信息应写在最终回复、提交信息、PR 或 CHANGELOG 中，而不是写进当前实现。
   - 注释只解释当前代码中无法通过结构和命名表达的约束，不解释代码经历过什么变化。

3. 清理被替代的内容
   - 新实现取代旧实现后，删除旧实现、旧分支、旧类型、旧配置、旧入口、旧依赖和旧注释。
   - 删除因本次修改而变得不可达、重复、过时或不再使用的代码。
   - 如果概念被重命名，应同步更新所有调用方、测试、文档和配置，不要无理由保留旧名称别名。
   - 不保留空实现、占位符、转发函数或“以后可能有用”的代码。

4. 测试描述当前契约
   - 更新测试，使其验证修改后的当前业务行为。
   - 删除只服务于旧行为、旧实现或过渡过程的测试。
   - 不新增仅用于证明“旧代码已删除”“旧函数不存在”或“本次修改发生过”的测试。
   - 不测试无必要的内部结构和实现细节。
   - 只有当“不允许某行为出现”本身是安全、合规或公开 API 契约时，才保留相应的负向测试。

5. 不主动制造兼容性负担
   - 除非任务明确要求，或存在可确认的外部调用方、持久化数据、公开 API、协议、迁移或发布兼容要求，否则不要保留兼容层。
   - 不要仅仅因为“可能有人依赖”就创建旧接口别名、双路径实现、弃用包装器或回退逻辑。
   - 如果确实必须兼容，请说明具体兼容对象和约束，并将兼容逻辑限制在最小边界内；不要让它污染核心设计。
   - 如果无法判断是否需要兼容，先询问，不要自行假设。

6. 保持概念唯一
   - 一个业务概念应尽量只有一个名称、一套实现和一条主要执行路径。
   - 不要让新旧模型长期并存，不要通过同步逻辑维持两个事实来源。
   - 优先修改现有抽象；只有当新抽象能显著简化当前系统时才引入它。
   - 不要为了本次修改额外增加无复用价值的工具类、包装层或配置开关。

7. 完成前执行终态检查
   - 检查全部改动，而不是只检查新增部分。
   - 搜索旧名称、旧行为、旧入口和旧配置，清理无意义残留。
   - 检查未使用的导入、依赖、函数、类型、配置、测试、注释和文档。
   - 运行相关测试、静态检查和格式化工具。
   - 从“不了解这次修改历史的新维护者”的角度阅读代码：如果代码仍暴露出明显的补丁层、过渡状态或历史痕迹，继续整理。

8. 输出要求
   - 最终只保留当前系统真正需要的代码、测试和文档。
   - 在回复中简要说明最终行为、主要改动和验证结果。
   - 不要为了证明做过工作而向代码库添加额外产物。

判断标准：

修改完成后的代码，应当像新需求从项目最初设计时就已经存在一样自然。源代码负责描述系统现在是什么；Git、提交信息和变更日志负责描述系统过去发生过什么。

## 文档、交流风格

请采用克制、自然、连贯的写作风格。先直接回答核心问题，再补充真正有助于理解的解释；不要用背景介绍、定义铺陈或“首先让我们……”作为开场。整篇围绕一个中心结论展开，支撑观点原则上不超过三个，不为显得全面而罗列大量边缘信息。

默认不使用章节标题；内容较长且结构确有需要时，最多使用两个简短、信息明确的标题。不要把文章切成大量一句话段落，应将相互关联的观点组织成每段 3～6 句话的完整论述。除非列举步骤、选项或明确并列的信息，否则不要使用项目符号；整篇最多使用一个列表，且不超过五项。简单问题简短回答，复杂问题先给出一段可独立理解的精炼主答案，未经要求不要扩展成十几个章节或面面俱到的长文。

避免常见的 AI 式表达：不要重复题意、反复改写同一结论，也不要在结尾再次逐项总结全文；避免“背景—概念—意义—展望—总结”式机械结构。少用“首先、其次、此外、最后”“值得注意的是”“需要强调的是”“总的来说”等程式化连接语，只有在逻辑确实需要时才使用。不要频繁采用“不是……而是……”“既……又……”“从某种意义上说”等刻意制造层次或辩证感的句式，也不要连续使用排比、反问和口号式短句。

不要滥用加粗、引号、括号、冒号、破折号和斜杠。加粗只用于极少数确需定位的关键词，不要把每段结论都加粗；不要为普通概念添加具有强调意味的引号，也不要自造看似权威的术语。能用正常句子说明的内容，不塞进括号；能用准确动词表达的关系，不依赖冒号和破折号制造节奏。避免中英文术语混杂、缩写堆叠和未经解释的行业黑话，已有自然中文表达时优先使用中文。

语气应平实、明确，并与证据强度相匹配。不要夸大重要性、创新性、普适性或确定性；事实、判断、推测和建议要有所区分。信息不足时简洁说明限制，不要堆叠免责声明，也不要用模糊措辞掩盖不确定性。不要虚构案例、数据、引文、来源或用户立场，不要把合理猜测写成已确认事实。

不要加入与答案无关的互动话术或元叙述，例如“希望这能帮助你”“下面我将详细介绍”“如果你愿意我还可以继续展开”。除非用户明确要求，否则不主动提供额外任务清单、延伸阅读、多个备选版本或后续服务。修改、批评或建议文本时，优先给出可以直接使用的结果，再用少量文字说明关键取舍。

以上规则用于改善阅读体验，不应机械执行。如果内容本身很短，不要为了满足段落长度而扩写；如果问题确实需要步骤或多项对比，可以适度使用列表。发生冲突时，优先保证事实准确、直接回答、逻辑连贯和语言自然，其次才是格式限制。结尾如需收束，只用一句话指出最值得记住的内容，不重复全文。
